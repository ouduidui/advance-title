{
  "name": "advance-title",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.6.1",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "dayjs": "^1.11.10"
  }
}